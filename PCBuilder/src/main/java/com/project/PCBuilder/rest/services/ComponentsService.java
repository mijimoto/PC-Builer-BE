/*
 * Created on 2025-07-25 ( 09:11:34 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 3.3.0
 */
package com.project.PCBuilder.rest.services;

import java.util.List;
import java.util.Optional;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.project.PCBuilder.persistence.entities.Components;
import com.project.PCBuilder.persistence.repositories.ComponentsRepository;
import com.project.PCBuilder.rest.dto.ComponentsDTO;
import com.project.PCBuilder.rest.services.commons.GenericService;

/**
 * REST service for entity "Components" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class ComponentsService extends GenericService<Components, ComponentsDTO> {

	private static final Logger logger = LoggerFactory.getLogger(ComponentsService.class);

	private final ComponentsRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public ComponentsService(ComponentsRepository repository) {
		super(Components.class, ComponentsDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private Integer getEntityId(ComponentsDTO dto) {
		return dto.getComponentid();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<ComponentsDTO> findAll() {
		logger.debug("findAll()");
		Iterable<Components> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param componentid 
	 * @return the entity or null if not found
	 */
	public ComponentsDTO findById(Integer componentid) {
		Integer entityId = componentid;
		logger.debug("findById({})", entityId);
		Optional<Components> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param componentid 
	 * @param dto 
	 */
	public void save(Integer componentid, ComponentsDTO dto) {
		Integer entityId = componentid;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setComponentid(componentid);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(ComponentsDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param componentid 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(Integer componentid, ComponentsDTO dto) {
		Integer entityId = componentid;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<Components> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			Components entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(ComponentsDTO dto) {
		logger.debug("create({})", dto);
		// auto-generated Primary Key
		repository.save(dtoToEntity(dto));
		return true; // always created
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param componentid 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(Integer componentid) {
		Integer entityId = componentid;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<ComponentsDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<Components> list = repository.findByTitle(title);
		List<Components> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<ComponentsDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<Components> list = repository.findByTitle(title);
		List<Components> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<ComponentsDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<Components> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}