/*
 * Created on 2025-07-25 ( 09:11:34 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 3.3.0
 */
package com.project.PCBuilder.rest.services;

import java.util.List;
import java.util.Optional;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.project.PCBuilder.persistence.entities.Motherboard;
import com.project.PCBuilder.persistence.repositories.MotherboardRepository;
import com.project.PCBuilder.rest.dto.MotherboardDTO;
import com.project.PCBuilder.rest.services.commons.GenericService;

/**
 * REST service for entity "Motherboard" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class MotherboardService extends GenericService<Motherboard, MotherboardDTO> {

	private static final Logger logger = LoggerFactory.getLogger(MotherboardService.class);

	private final MotherboardRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public MotherboardService(MotherboardRepository repository) {
		super(Motherboard.class, MotherboardDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private Integer getEntityId(MotherboardDTO dto) {
		return dto.getPartid();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<MotherboardDTO> findAll() {
		logger.debug("findAll()");
		Iterable<Motherboard> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param partid 
	 * @return the entity or null if not found
	 */
	public MotherboardDTO findById(Integer partid) {
		Integer entityId = partid;
		logger.debug("findById({})", entityId);
		Optional<Motherboard> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param partid 
	 * @param dto 
	 */
	public void save(Integer partid, MotherboardDTO dto) {
		Integer entityId = partid;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setPartid(partid);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(MotherboardDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param partid 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(Integer partid, MotherboardDTO dto) {
		Integer entityId = partid;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<Motherboard> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			Motherboard entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(MotherboardDTO dto) {
		logger.debug("create({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			return false; // already exists, not created
		} else {
			repository.save(dtoToEntity(dto));
			return true; // created
		}
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param partid 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(Integer partid) {
		Integer entityId = partid;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<MotherboardDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<Motherboard> list = repository.findByTitle(title);
		List<Motherboard> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<MotherboardDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<Motherboard> list = repository.findByTitle(title);
		List<Motherboard> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<MotherboardDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<Motherboard> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}